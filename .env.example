# ImpossibleMA — environment template
#
# Copy this file to `.env` and fill in real values before running.
# Never commit your .env (it is gitignored).

# Random secret used to HMAC-sign TruthCert bundles. NEVER commit this; generate with `python -c 'import secrets; print(secrets.token_hex(32))'`
TRUTHCERT_HMAC_KEY=
